import React from 'react';
import '../App.css';

const Montage = ({ photos }) => {
  // Logic to create montage (similar to previous code)

  return (
    <div className="montage">
      <h2>Montage Timelapse</h2>
      <div className="photo-grid">
        {/* Display photos */}
      </div>
    </div>
  );
};

export default Montage;
